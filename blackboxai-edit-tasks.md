# blackboxai-edit-tasks.md

## Root (vanilla)
- Refactor `script.js` for readability without changing functionality
- Add a small safety net for `transitionend` not firing
- Remove/avoid duplicate event listener attachments where possible

## React (`portfolio-react/`)
- Ensure code builds; fix any syntax/runtime/logical errors
- Delete unwanted/dead/unneeded files if any are discovered

## Verification
- Root manual run check (navigation + contact form)
- `portfolio-react` build

